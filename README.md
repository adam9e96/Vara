# Vara

Windows 환경 변수를 GUI로 조회, 추가, 수정, 삭제, 백업, 복원할 수 있는 Electron 데스크톱 앱입니다.

## 소개

Vara는 Windows 환경 변수 관리를 터미널이나 시스템 설정 화면 대신 한 곳에서 처리할 수 있도록 만든 앱입니다.

주요 기능:

- 사용자 환경 변수 / 시스템 환경 변수 조회
- 환경 변수 추가, 수정, 삭제
- `PATH` 같은 세미콜론(`;`) 기반 다중 값 편집
- 드래그 앤 드롭으로 다중 값 순서 변경
- 환경 변수 백업 및 복원